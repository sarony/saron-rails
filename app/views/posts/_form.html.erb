<%= form_for(@post) do |f| %>
  <%= f.text_field :title, :placeholder => "title" %> <br>
  <%= f.text_area :preview, :placeholder => "preview" %></br>
  <%= f.text_area :body, :class => "post_body", :placeholder => "body" %></br>
  <%= f.submit %>
<% end %>